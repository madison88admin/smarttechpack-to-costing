import fs from "node:fs"; import { createClient } from "@supabase/supabase-js";
for(const l of fs.readFileSync(".env.local","utf8").split(/\r?\n/)){const m=l.match(/^([A-Z0-9_]+)=(.*)$/);if(m)process.env[m[1]]=m[2].replace(/^"|"$/g,"");}
const s=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.SUPABASE_SECRET_KEY,{auth:{persistSession:false}}).schema("tp_costing"); const {data:req}=await s.from("costing_requests").select("id,factory_cbds(id,raw_payload)").eq("request_number","CR-069113").single(); const cbd=Array.isArray(req.factory_cbds)?req.factory_cbds[0]:req.factory_cbds; const yarnLines=[{name:"(MMPP12500-00WG) 100% Recycled Polyester 1/12.5",consumption:114,materialPrice:6.49,materialCost:0.73986}]; const trimLines=[{name:"Sewing Thread",consumption:1,materialPrice:0.01,materialCost:0.01}]; const knittingLines=[{machineType:"Flat-7GG",knittingTime:9,sah:0.05,knittingCost:0.45}]; const operationsLines=[{operation:"Labeling",operationCost:0.1},{operation:"Overlock (Locked End/ Manual/ 4-6 Dart)",operationCost:0.1},{operation:"4 Gore Closing (Non Reversibile)",operationCost:0.1},{operation:"Neaten/Steaming/Packing (Beanie)",operationCost:0.4},{operation:"Linking Beanie (Flat/ 1 Layer/ Cuff)",operationCost:0.17}]; const p={...(cbd.raw_payload||{}),yarnLines,trimLines,knittingLines,operationsLines,materialTotal:0.74986,knittingTotal:0.45,operationsTotal:0.87,packagingTotal:0.15,overheadProfitTotal:0.45,standardPackagingCost:0.15,specialPackagingCost:0,overheadCost:0.25,profitCost:0.2,factoryCostTotal:2.66986,grandTotal:2.66986,laborCost:0.87}; const {error}=await s.from("factory_cbds").update({raw_payload:p}).eq("id",cbd.id); if(error)throw error; console.log(JSON.stringify({ok:true,request:req.id,cbd:cbd.id,total:2.66986}));
